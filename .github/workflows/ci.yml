name: CI

on:
  push:
    branches: [main, authery]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt -p authery --check
      - run: cargo clippy -p authery --all-features -- -D warnings
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-rdme
      # README.md is generated from the crate docs in src/lib.rs.
      - run: cargo rdme --check

  feature-matrix:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        features:
          - ""
          - "password"
          - "email"
          - "oauth"
          - "webauthn"
          - "otp,mfa,password"
          - "totp,mfa,password"
          - "sms"
          - "sms,mfa"
          - "oauth,axum,pages"
          - "oauth,axum,pages,user,email,password,otp,webauthn,mfa,totp,sms"
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo build -p authery --no-default-features --features "${{ matrix.features }}"

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      # Core flow tests (no external services needed).
      - run: cargo test -p authery --no-default-features --features password,email,otp,mfa,user,totp,sms
      # Default features; the Keycloak-dependent test self-skips when it is
      # not reachable.
      - run: cargo test -p authery

  examples:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo build --manifest-path examples/memory-store/Cargo.toml
      - run: cargo build --manifest-path examples/memory-store-password-only-no-templates/Cargo.toml
      - run: cargo build --manifest-path examples/postgres-store/Cargo.toml
